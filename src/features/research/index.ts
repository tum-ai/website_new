/**
 * Research public API for other features: the brand placeholder, which
 * projects shows for cards without an image until the design system provides
 * a brand panel.
 *
 * The /research route imports `./research-page` directly; never re-export a
 * page here (see features/partners/index.ts).
 */

export { BrandPlaceholder } from "./brand-placeholder";
