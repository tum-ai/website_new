/**
 * Design-system feature: the /design-system showcase (development and
 * preview deployments only). Other code imports only from here.
 */

export { DesignSystemPage } from "./design-system-page";
